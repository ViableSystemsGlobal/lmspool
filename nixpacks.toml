[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "npm ci",
  "ls node_modules | grep -E '(tailwindcss|postcss)' || echo 'WARNING: tailwindcss or postcss not found'",
  "npx prisma generate",
  "npm run build"
]

[start]
cmd = "npm run start"

